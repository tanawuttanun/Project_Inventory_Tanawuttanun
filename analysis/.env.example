# ปลายทางที่สคริปต์จะไปดึง Combined Dataset (ค่าเริ่มต้นคือ Aggregation API)
AGGREGATOR_URL=http://localhost:6000/api/combined-products

# ถ้าจะข้าม Aggregator แล้วยิงตรงเข้า API ของตัวเอง ใช้:
# AGGREGATOR_URL=http://localhost:3021/api/products

# ใส่เมื่อ API ปลายทางตั้ง x-api-key ไว้
MEMBER_API_KEY=
